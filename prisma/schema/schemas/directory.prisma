model Directory {
  id          String           @id @default(cuid()) @map("_id")
  name        String
  description String?
  fields      DirectoryField[]
  elements    Elements[]
  layers      Layer[]
}
